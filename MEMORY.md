# MEMORY

Start here:

- `state.md`: current project state, latest verified numbers, and next actions.
- `findings.md`: experiment conclusions and evidence.
- `gotchas.md`: operational traps that have already cost jobs.
- `README.md`: public-facing project overview and running instructions.

Locked story:

The MSH anchor gap is driven by analysis/synthesis depth, not by training length or latent width. Depth `[2,2,2]` nearly reaches vanilla but pays a large rate cost, so depth defines the RD frontier. Real arithmetic coding is now valid after symmetric scale snapping before `build_indexes(scales)`.

